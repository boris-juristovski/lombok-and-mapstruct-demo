package examples.mapstruct.v11;

//@Mapper
interface CountryMapper {

    // do not have spring security on classpath
//    CountryEntity fromModel(Country model, @Context SecurityContext securityContext);

//    Collection<CountryEntity> fromModels(Collection<Country> models, @Context SecurityContext securityContext);
}
